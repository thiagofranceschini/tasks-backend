pipeline {
    agent any
    stages {
        stage('firstTest') {
            steps {
                sh 'echo *** INICIO ***'
            }
            steps {
                sh 'echo executando o teste de build utilizando o Jenkinsfile'
            }
        }
    }
}